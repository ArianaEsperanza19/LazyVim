// 1. Usa 'J' para unir estas líneas y formar una frase
La programación
es el arte
de decirle a una
computadora qué hacer.

// 2. Usa 'f' para buscar la letra "z" y luego ';' para saltar a las siguientes
zapato azul, zorro audaz, zigzag veloz, manzana roja, luz brillante.

// 3. Usa 'R' (Modo Reemplazar) para cambiar "ERRORES" por "CORRECTO"
ERRORES ERRORES

// 4. Usa 'ce' (cambiar hasta el final de palabra) o 'cb' (atrás)
Sustituye esta_palabra_fea por "Genial".

// 5. Salta entre paréntesis y llaves con '%'
function holaMundo() {
    if (verdadero) {
        console.log("Vim es potente");
    }
}

// 6. Usa 'ci{' para limpiar el contenido de este objeto
const configuracion = {
    usuario: "admin",
    clave: "12345",
    rol: "superman"
}

// 7. Usa '<<' y '>>' para arreglar la sangría (indentación)
        Esta línea está muy a la derecha.
Esta línea está muy a la izquierda.

// 8. Navegación rápida:
// Usa '1G' para ir al inicio.
// Usa 'G' para ir al final.
// Usa '15%' para saltar al 15 por ciento del archivo.
